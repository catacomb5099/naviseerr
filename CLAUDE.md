# Claude Instructions

Read `AGENTS.md` first. It is the source of truth for Naviseerr's product context, architecture direction, coding expectations, and agent workflow.

Keep `CLAUDE.md` intentionally small. If durable project guidance changes, update `AGENTS.md` rather than duplicating it here.
